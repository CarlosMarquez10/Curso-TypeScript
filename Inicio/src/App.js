"use strict";
let Data;
Data = "Inicio del curso con TypeScript";
let ContRoot = document.getElementById("container-root");
ContRoot === null || ContRoot === void 0 ? void 0 : ContRoot.innerHTML = `<h1>${Data}</h1>`;
